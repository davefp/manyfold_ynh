**WORK IN PROGRESS, DO NOT USE!**

This repo is a packaging of Manyfold for Yunohost. It is based on the mastodon_ynh repo, and is in the process of being adapted to manage Manyfold instead

## Tasks

Before even starting to run tests, the following needs to happen:

- [x] Initial culling of files
- [x] Manifest file
- [x] Initial `install` script rewrite
- [x] Env file rewrite
- [x] Service file rewrites
- [x] Initial `upgrade` script rewrite
- [x] Initial `backup` script rewrite
- [x] Initial `remove` script rewrite
- [ ] nginx config rewrite

Then I can start testing and fixing the (presumably numerous) bugs.
